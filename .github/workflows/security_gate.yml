name: security_gate

on: 
  push:
jobs:
  security_check:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v4
        - name: check forbidden file
          run: |
            [ -f passwords.txt ] && exit 1 || exit 0
